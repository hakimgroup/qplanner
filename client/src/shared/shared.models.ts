export enum AppRoutes {
	Home = "/",
	Login = "/login",
	Dashboard = "/dashboard",
	Admin = "/admin",
}

export enum DatabaseTables {
	Users = "users",
	Campaigns = "campaigns",
	CampaignsList = "campaigns_list",
}
